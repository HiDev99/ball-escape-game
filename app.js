(()=>{'use strict';const $=id=>document.getElementById(id),c=$('game');if(!c)return;const ctx=c.getContext('2d'),T=Math.PI*2,X=360,Y=640,C=['#ff4d73','#4d8dff','#ffe05b','#5dffae','#bd79ff','#ff994d','#4deeea','#ff70d1'];let balls=[],run=true,last=performance.now(),time=0,ang=-Math.PI/2,audio=null,rec=null,chunks=[];const q=()=>({m:$('mode').value,n:+$('count').value,s:+$('speed').value,g:+$('gravity').value,gap:+$('gap').value*Math.PI/180,rot:+$('rotation').value*.35,r:+$('size').value,tr:$('trails').checked,snd:$('sound').checked,col:$('collisions').checked});function mk(i){let a=Math.random()*T,s=q().s*60;return{x:X+Math.cos(a)*100,y:Y+Math.sin(a)*100,vx:Math.cos(a+1.25)*s,vy:Math.sin(a+1.25)*s,r:q().r,c:C[i%C.length],trail:[],hits:0}}function reset(){balls=Array.from({length:q().n},(_,i)=>mk(i));time=0;ang=-Math.PI/2;run=true;$('play').textContent='Ⅱ Pause'}function tone(f=300){if(!q().snd)return;try{audio??=new(window.AudioContext||window.webkitAudioContext)();if(audio.state==='suspended')audio.resume();let o=audio.createOscillator(),g=audio.createGain();o.type='triangle';o.frequency.value=f;g.gain.value=.018;o.connect(g);g.connect(audio.destination);o.start();g.gain.exponentialRampToValueAtTime(.0001,audio.currentTime+.06);o.stop(audio.currentTime+.065)}catch(e){}}function gap(a,z){return Math.abs(Math.atan2(Math.sin(a-ang),Math.cos(a-ang)))<z.gap/2}function upd(b,z,dt){let G=z.g*1200;if(z.m==='reverse')G=-G;if(z.m==='zero')G=0;if(z.m==='gravity')G*=Math.sin(time*.003)*2;b.vy+=G*dt;b.x+=b.vx*dt;b.y+=b.vy*dt;let dx=b.x-X,dy=b.y-Y,d=Math.hypot(dx,dy)||1,R=275-b.r;if(d>=R){let a=Math.atan2(dy,dx);if(gap(a,z)){b.dead=true;b.trail.length=0;tone(800);return}let nx=dx/d,ny=dy/d,dot=b.vx*nx+b.vy*ny;b.vx-=2*dot*nx;b.vy-=2*dot*ny;b.x=X+nx*(R-1);b.y=Y+ny*(R-1);b.hits++;if(z.m==='speed'){b.vx*=1.04;b.vy*=1.04}if(z.m==='pinball'){b.vx*=1.07;b.vy*=1.07}if(z.m==='grow')b.r=Math.min(48,b.r+1);tone(190+(b.hits%10)*28)}if(z.tr){b.trail.push([b.x,b.y]);if(b.trail.length>24)b.trail.shift()}else b.trail.length=0}function collide(z){if(!z.col)return;for(let i=0;i<balls.length;i++)for(let j=i+1;j<balls.length;j++){let a=balls[i],b=balls[j],dx=b.x-a.x,dy=b.y-a.y,d=Math.hypot(dx,dy),m=a.r+b.r;if(d>0&&d<m){let nx=dx/d,ny=dy/d,o=m-d;a.x-=nx*o/2;a.y-=ny*o/2;b.x+=nx*o/2;b.y+=ny*o/2;let rv=(b.vx-a.vx)*nx+(b.vy-a.vy)*ny;if(rv<0){a.vx+=rv*nx;a.vy+=rv*ny;b.vx-=rv*nx;b.vy-=rv*ny}}}}function step(dt){let z=q();time+=dt*1000;ang+=z.rot*dt;for(let b of balls)upd(b,z,dt);collide(z);balls=balls.filter(b=>!b.dead);if((z.m==='multiply'||z.m==='collision')&&balls.length&&balls.length<30&&Math.floor(time/3000)>balls.length-z.n)balls.push(mk(balls.length))}function draw(){let z=q(),bg=ctx.createRadialGradient(X,Y,20,X,Y,560);bg.addColorStop(0,'#151936');bg.addColorStop(.55,'#080a17');bg.addColorStop(1,'#020309');ctx.fillStyle=bg;ctx.fillRect(0,0,720,1280);ctx.textAlign='center';ctx.fillStyle='#fff';ctx.font='900 36px system-ui';ctx.fillText(($('mode').selectedOptions[0]?.textContent||'BALL ESCAPE').toUpperCase(),X,100);ctx.fillStyle='#9ba5c4';ctx.font='700 19px system-ui';ctx.fillText((time/1000).toFixed(1)+'s • '+balls.length+' ACTIVE',X,140);ctx.save();ctx.translate(X,Y);ctx.rotate(ang);let gr=ctx.createLinearGradient(-275,0,275,0);gr.addColorStop(0,'#58eaff');gr.addColorStop(.5,'#fff');gr.addColorStop(1,'#a77cff');ctx.strokeStyle=gr;ctx.lineWidth=11;ctx.lineCap='round';ctx.shadowColor='#826fff';ctx.shadowBlur=20;ctx.beginPath();ctx.arc(0,0,275,z.gap/2,T-z.gap/2);ctx.stroke();ctx.restore();for(let b of balls){if(z.tr)for(let i=0;i<b.trail.length;i++){let p=b.trail[i],a=(i+1)/b.trail.length;ctx.globalAlpha=a*.35;ctx.fillStyle=b.c;ctx.beginPath();ctx.arc(p[0],p[1],Math.max(2,b.r*a*.65),0,T);ctx.fill()}ctx.globalAlpha=1;ctx.fillStyle=b.c;ctx.shadowColor=b.c;ctx.shadowBlur=20;ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,T);ctx.fill();ctx.shadowBlur=0}ctx.globalAlpha=1}function loop(now){let dt=Math.min(.025,(now-last)/1000);last=now;if(run){let n=Math.max(1,Math.ceil(dt/.006));for(let i=0;i<n;i++)step(dt/n)}draw();requestAnimationFrame(loop)}function labels(){['count','speed','gravity','gap','rotation','size'].forEach(id=>{let el=$(id+'V');if(el)el.textContent=$(id).value})}document.querySelectorAll('input[type=range]').forEach(e=>e.addEventListener('input',labels));$('mode').addEventListener('change',reset);$('reset').onclick=reset;$('play').onclick=()=>{run=!run;$('play').textContent=run?'Ⅱ Pause':'▶ Play'};$('random').onclick=()=>{$('mode').selectedIndex=Math.floor(Math.random()*$('mode').options.length);$('count').value=3+Math.floor(Math.random()*6);$('speed').value=5+Math.floor(Math.random()*5);$('gap').value=25+Math.floor(Math.random()*25);labels();reset()};if($('soundTest'))$('soundTest').onclick=()=>{tone(440);setTimeout(()=>tone(660),80)};document.querySelectorAll('[data-preset]').forEach(b=>b.onclick=()=>{let v=b.dataset.preset==='calm'?[3,5,.1,45,.4,15]:b.dataset.preset==='viral'?[6,7,.15,32,.7,14]:[10,10,.25,25,1.5,11];['count','speed','gravity','gap','rotation','size'].forEach((id,i)=>$(id).value=v[i]);labels();reset()});$('record').onclick=()=>{if(rec&&rec.state==='recording'){rec.stop();return}if(typeof MediaRecorder==='undefined'||!c.captureStream){alert('Recording is not supported in this browser.');return}try{let stream=c.captureStream(60);chunks=[];rec=new MediaRecorder(stream);rec.ondataavailable=e=>{if(e.data.size)chunks.push(e.data)};rec.onstop=()=>{let blob=new Blob(chunks,{type:'video/webm'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='ball-escape-'+Date.now()+'.webm';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),3000);$('record').textContent='● Record + Sound'};rec.start();$('record').textContent='■ Stop & Download'}catch(e){alert('Recording could not start.')}};labels();reset();requestAnimationFrame(loop)})();